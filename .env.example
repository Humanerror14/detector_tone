# Frontend
VITE_YOUTUBE_API_KEY=
# For running outside docker (frontend -> backend)
VITE_ANALYSIS_API_URL=http://localhost:8000

# Backend
YOUTUBE_LYRICS_LANGS=id,en
# Optional: if yt-dlp needs explicit deno path on Windows (not needed in container)
YTDLP_DENO_PATH=
